# Makefile to compile the module in this directory

SOURCES = $(wildcard db/sql/*.cpp)
LIBRARIES = dbsql
DYNAMIC_LINK_LIBRARIES = dbrt dbutil dbnet
DYNAMIC_MACOS_LIBRARIES = dbio dbmodest dbcrypto

# ----------- Standard Makefile
include @DBCOREDIR@/setup/Makefile.base
