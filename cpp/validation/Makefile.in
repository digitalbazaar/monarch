# Makefile to compile the module in this directory

SOURCES = $(wildcard db/validation/*.cpp)
LIBRARIES = dbvalidation
DYNAMIC_LINK_LIBRARIES = dbrt dbutil

# ----------- Standard Makefile
include @DBCOREDIR@/setup/Makefile.base

