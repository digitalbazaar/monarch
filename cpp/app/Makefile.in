# Makefile to compile the module in this directory

HEADERS = $(wildcard *.h)
SOURCES = $(wildcard *.cpp)
LIBRARIES = moapp
DYNAMIC_LINK_LIBRARIES = mort mologging moio mocrypto monet modata moutil moconfig

DYNAMIC_WINDOWS_LIBRARIES = pthreadGCE2 mgwz libgnurx-0 libeay32 ssleay32
STATIC_WINDOWS_LIBRARIES = pthreadGCE2 z libgnurx-0
EXTRA_WINDOWS_LIBRARIES = -lws2_32
DYNAMIC_MACOS_LIBRARIES = crypto ssl momodest mocompress

# ----------- Standard Makefile
include @MONARCH_DIR@/setup/Makefile.base

