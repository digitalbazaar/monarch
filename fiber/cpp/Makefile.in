# Makefile to compile the module in this directory

SOURCES = $(wildcard db/fiber/*.cpp)

# If we're building on MacOS, include the get/setcontext assembler files
ifeq (@BUILD_FOR_MACOS@,yes)
SOURCES += db/fiber/PortableUContextAsm.S
endif

LIBRARIES = dbfiber
DYNAMIC_LINK_LIBRARIES = dbrt dbmodest

# ----------- Standard Makefile
include @DBCOREDIR@/setup/Makefile.base

