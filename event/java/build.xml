<!DOCTYPE project [
  <!ENTITY jaxrpctargets SYSTEM "../../setup/targets.xml">
]>

<project name="event" default="build" basedir=".">
	<property file="../../setup/build.properties"/>
	<property file="build.properties"/>

  &jaxrpctargets;

  <target name="build" depends=""
    description="Builds the Digital Bazaar Event library">
		<mkdir dir="${build}/com/db/event"/>
		<mkdir dir="${dist}"/>
		<mkdir dir="${test}"/>
		<javac srcdir="${event}/src/com/db/event"
      debug="${debug}"
      debuglevel="${debuglevel}"
      destdir="${build}"
      includes="**/*.java">
			<classpath refid="compile.classpath"/>
		</javac>
		<jar jarfile="${dist}/dbevent.jar">
			<fileset dir="build" includes="com/db/event/**/*"/>
		</jar>
  </target>

	<target name="run-tests" 
depends="build"
    description="Runs the unit tests for the library">
	</target>

</project>
