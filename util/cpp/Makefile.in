# Makefile to compile the module in this directory

SOURCES = $(wildcard db/util/*.cpp) \
          $(wildcard db/util/regex/*.cpp)
LIBRARIES = dbutil

# ----------- Standard Makefile
include @DBCOREDIR@/setup/Makefile.base

