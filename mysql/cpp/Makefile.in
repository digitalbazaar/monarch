# Makefile to compile the module in this directory

ifeq (@BUILD_MYSQL@,yes)
SOURCES = $(wildcard db/sql/mysql/*.cpp)
LIBRARIES = dbmysql
DYNAMIC_LINK_LIBRARIES = dbrt dbnet dbsql
DYNAMIC_WINDOWS_LIBRARIES = mysql
endif

# ----------- Standard Makefile
include @DBCOREDIR@/setup/Makefile.base
